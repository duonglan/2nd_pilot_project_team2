<% provide(:title, 'All users') %>
<h1>All Group</h1>
<%#= will_paginate %>
<ul class="teams">
  <% @groups.each do |group| %>
    <li>
      <%= link_to group.name, group %>
      (by <%= group.owner.name %>) (<%= group.members.count%>)
    </li>
  <% end %>
</ul>
<%#= will_paginate %>