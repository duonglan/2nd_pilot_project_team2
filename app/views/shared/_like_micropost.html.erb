<% if micropost.like_micropost?(current_user) %>
  <% micropost.like_microposts.each do |f| %>
    <% if f.user_id == current_user.id %>
      <%= link_to "unlike", user_micropost_like_micropost_path(@user, micropost, f), method: :delete %>
    <% end %>
  <% end %>
<% else %>
  <%= link_to "like", user_micropost_path(@user, micropost, like: "like_micropost"), method: :patch %>
<% end %>
<% if micropost.has_like? %>
  <%= image_tag "like.ico" %>
  <%= micropost.like_microposts.count %> person like this
<% end %>
<%= link_to "share", user_micropost_path(current_user, micropost, share: "share"), method: :patch, data: { confirm: micropost.content } %>